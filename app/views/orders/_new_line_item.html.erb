<%= fields_for "order[line_items_attributes][#{Time.now.to_i}]", order.line_items.new, child_index: Time.now.to_i do |line_item_form| %>
  <%= render "line_items/form", _form: line_item_form %>
<% end %> 